# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.turbo/
.angular/
*.tsbuildinfo

# Env
.env
.env.*
!.env.example

# OS
.DS_Store

# Editor
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage/
*.log

# Drizzle local
apps/api/drizzle/meta/_journal.json.bak

# Typst temp workspaces
/tmp/typst-*
.superpowers/

# Pinned local typst binary (apps/api/scripts/install-typst-dev.sh)
apps/api/.typst-bin/

# Local tool state (machine-specific, rebuilt locally — never shared)
.claude/
.project-brain/

# Local object storage data (native MinIO run outside docker)
minio-data/

# Playwright MCP session artifacts + ad-hoc smoke screenshots (root-level)
.playwright-mcp/
/*.png
