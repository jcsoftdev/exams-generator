{
  "name": "@exams-generator/shared",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "echo \"(no lint configured for shared)\" && exit 0",
    "test": "echo \"(no tests configured for shared)\" && exit 0"
  }
}
