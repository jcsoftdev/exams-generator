import type { CreateTenantPayload } from "@exams-generator/shared";

/** `POST /tenants` request body — wire contract lives in `@exams-generator/shared` (audit 2026-08-21, M4b). */
export type CreateTenantDto = CreateTenantPayload;
