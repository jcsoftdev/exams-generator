/** DI token for the `QuestionGeneratorPort` implementation the ai module uses. */
export const QUESTION_GENERATOR_PORT = Symbol("QuestionGeneratorPort");

/** DI token for the `ImageCropperPort` implementation the ai module uses. */
export const IMAGE_CROPPER_PORT = Symbol("ImageCropperPort");
